services:
  boma:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: boma_analytics
    ports:
      - "8888:8888"
    volumes:
      - .:/opt/app
    working_dir: /opt/app
    entrypoint: jupyter notebook --ip=0.0.0.0 --port=8888 --allow-root --no-browser
